# Raw data (FNSPID ~30GB) — never in git; download separately
DATA/

# Model weights (FinBERT ~1.3GB) — fetch from HuggingFace
models/

# Copyrighted reference PDFs — do not redistribute
Related Works/

# Large derived artifacts (reproducible from scripts+DATA)
*.parquet

# Run logs / temp
universe/_*.txt
panel/_*.txt
*.log
**/__pycache__/
*.pyc
.hf/
.torch/
.cache/
.pipcache/
.DS_Store

# ETT public benchmark (reproducible via curl from zhouhaoyi/ETDataset)
bench/*.csv

# bench transient run logs (keep the .md reports)
bench/_*.txt
.smoke/
p2out/
bench/phase2_results.md
.claude/
